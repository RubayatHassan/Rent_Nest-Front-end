"use client";
import { useEffect, useState } from "react";
import { getAdminUsers, updateUserStatus, User } from "../../../lib/api";
export default function Page(){const [items,setItems]=useState<User[]>([]);const [error,setError]=useState("");const load=()=>getAdminUsers("limit=100").then(r=>setItems(r.data||[])).catch(e=>setError(e instanceof Error?e.message:"Unable to load users"));useEffect(()=>{load()},[]);const toggle=async(user:User)=>{try{await updateUserStatus(user.id,user.activeStatus==="BLOCKED"?"ACTIVE":"BLOCKED");load()}catch(e){setError(e instanceof Error?e.message:"Unable to update user")}};return <div><div className="resource-head"><div><p className="eyebrow">Administration</p><h1>Users</h1></div></div>{error&&<p className="form-error">{error}</p>}<div className="panel table-panel"><table className="data-table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Action</th></tr></thead><tbody>{items.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.activeStatus}</td><td><button className="text-button" onClick={()=>toggle(u)}>{u.activeStatus==="BLOCKED"?"Unblock":"Block"}</button></td></tr>)}</tbody></table></div></div>}
