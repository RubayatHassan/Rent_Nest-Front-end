"use client";
import { useEffect, useState } from "react";
import { getAdminRentals, RentalRequest, RentalStatus, updateAdminRentalStatus } from "../../../lib/api";
export default function Page(){const [items,setItems]=useState<RentalRequest[]>([]);const [error,setError]=useState("");const load=()=>getAdminRentals("limit=100").then(r=>setItems(r.data||[])).catch(e=>setError(e instanceof Error?e.message:"Unable to load rentals"));useEffect(()=>{load()},[]);const update=async(id:string,status:RentalStatus)=>{try{await updateAdminRentalStatus(id,status);load()}catch(e){setError(e instanceof Error?e.message:"Unable to update rental")}};return <div><div className="resource-head"><div><p className="eyebrow">Rental operations</p><h1>Rentals</h1></div></div>{error&&<p className="form-error">{error}</p>}<div className="panel table-panel"><table className="data-table"><thead><tr><th>Property</th><th>Tenant</th><th>Status</th><th>Update</th></tr></thead><tbody>{items.map(r=><tr key={r.id}><td>{r.property?.title}</td><td>{r.tenant?.name}</td><td>{r.status}</td><td><select value={r.status} onChange={e=>update(r.id,e.target.value as RentalStatus)}><option>PENDING</option><option>APPROVED</option><option>REJECTED</option><option>ACTIVE</option><option>COMPLETED</option><option>CANCELLED</option></select></td></tr>)}</tbody></table></div></div>}
