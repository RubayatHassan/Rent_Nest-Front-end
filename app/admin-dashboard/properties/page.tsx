"use client";
import { Search } from "lucide-react";
import { useEffect, useState } from "react";
import { getAdminProperties, Property, updateAdminPropertyStatus } from "../../../lib/api";
export default function Page(){const [items,setItems]=useState<Property[]>([]);const [query,setQuery]=useState("");const [error,setError]=useState("");const load=()=>getAdminProperties("limit=100").then(r=>setItems(r.data||[])).catch(e=>setError(e instanceof Error?e.message:"Unable to load properties"));useEffect(()=>{load()},[]);const status=async(id:string,value:Property["status"])=>{try{await updateAdminPropertyStatus(id,value);load()}catch(e){setError(e instanceof Error?e.message:"Unable to update property")}};const filtered=items.filter(p=>`${p.title} ${p.landlord?.name||""} ${p.category?.name||""} ${p.location}`.toLowerCase().includes(query.toLowerCase()));return <div><div className="resource-head"><div><p className="eyebrow">Marketplace</p><h1>Properties</h1></div></div>{error&&<p className="form-error">{error}</p>}<div className="panel table-panel"><div className="table-tools"><div className="search-box"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search properties, landlords or categories..." /></div><span className="muted">{filtered.length} results</span></div><table className="data-table"><thead><tr><th>Property</th><th>Landlord</th><th>Category</th><th>Status</th><th>Action</th></tr></thead><tbody>{filtered.map(p=><tr key={p.id}><td>{p.title}</td><td>{p.landlord?.name}</td><td>{p.category?.name}</td><td>{p.status}</td><td><select value={p.status} onChange={e=>status(p.id,e.target.value as Property["status"])}><option>AVAILABLE</option><option>UNAVAILABLE</option><option>RENTED</option></select></td></tr>)}</tbody></table></div></div>}
